<div class="navbar navbar-expand-lg bg-light d-flex justify-content-between px-5 py-3 border-bottom">

<%= link_to "Wikinimous",
articles_path(@articles) %>

<%= link_to "New Article",
  new_article_path(@article) %>
  </div>

<ul class="container pt-5">
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %> <br>
      <%= article.content %>
    </li>
  <% end %>
</ul>
